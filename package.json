{
  "name": "proxytline-mcp",
  "version": "1.0.0",
  "description": "MCP server for Proxyline proxy provider API",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "proxytline-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "prepublishOnly": "npm run build",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
